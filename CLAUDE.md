# 台股個人分析平台 — CLAUDE.md

給 Claude Code 讀的專案說明。每次開始工作前，先讀完這份文件。

## 關於使用者

- 使用者是 Fukang。這是自用工具，不對外開放、不收費。
- 請用**繁體中文**回覆。
- 使用者不是全職工程師，但部署過 Vercel + Supabase + GitHub 的專案。每一步都請說明在做什麼；需要使用者操作的事（登入網站、設定環境變數、申請 Token），請列出具體步驟。
- 原則是**不能用猜的**。說「修好了」之前，要實際跑過測試或用真實資料驗證，並老實說明驗證到什麼程度、還有什麼沒驗證。

## 專案目標

參考 `dvorak0727/Genspark-Stock-AI` 的功能概念，重新設計一個乾淨、可維護、數據誠實的台股分析工具。

- 只參考功能概念，**不複製該 repo 的程式碼**。該 repo 沒有授權條款，預設保留所有權利。

## 核心原則（跟原版最大的不同）

1. **絕不用模擬或亂數資料填補真實資料。** 資料不足時，畫面明確顯示「資料不足」，計算時排除該筆。
2. **祕密只放伺服器端。** FinMind Token、Supabase service role key 只存在伺服器端環境變數，前端永遠拿不到。
3. **評分要標準化。** 法人買賣超要除以近 20 日平均成交量（或流通股數），不能直接用張數絕對值。
4. **評分要透明。** 每個分數都能展開，看到每一項的原始數值和加減分。同一個技術指標不能重複計分。
5. **不用誤導性名稱。** 不把規則式打分稱為「AI」，也不用「量子」「流體力學」這類物理名詞包裝。
6. **模組化。** 不准把所有邏輯塞在一個檔案。指標計算寫成純函式，而且必須有單元測試。
7. **中性描述。** 畫面不顯示「建議買入／賣出」，改用中性描述，例如「偏多訊號 4 項／偏空訊號 1 項」。

## 技術架構

- **前端：** Vue 3 + Vite。
  - K 線圖用 TradingView lightweight-charts（Apache-2.0，保留官方要求的標示）。
  - 其他圖表用 Chart.js。
- **後端：** Vercel Serverless Functions（Node.js），放在 `/api`。
- **資料庫：** Supabase（PostgreSQL）。
  - 新開一個專案，**不要跟 Gemba Walk 共用**。
- **資料抓取：** GitHub Actions 在每個交易日收盤後排程執行腳本，從 FinMind 抓資料寫進 Supabase。
  - 前端只讀自己的資料庫，不直接呼叫 FinMind。
- **登入：** Supabase Auth，只開放使用者本人一個帳號。所有資料表都開啟 RLS。
- **測試：** Vitest。

## 資料來源（FinMind）

**先用這幾個 dataset：**
- `TaiwanStockInfo`：股票清單
- `TaiwanStockPrice`：日 K
- `TaiwanStockInstitutionalInvestorsBuySell`：三大法人
- `TaiwanStockMarginPurchaseShortSale`：融資融券

**之後視需要加入：**
- `TaiwanStockPER`
- `TaiwanStockMonthRevenue`
- `TaiwanStockHoldingSharesPer`（千張大戶）

**注意事項：**
- 使用前先查 FinMind 官方文件，確認 dataset 名稱和欄位，不要憑記憶寫。
- 免費帳號有流量限制，只抓「追蹤股票池」（先控制在 50 檔以內）。
- 歷史資料首次回補完成後，每天只抓新增的資料。

## 資料表初步規劃

| 資料表 | 內容 |
|---|---|
| `stocks` | 代號、名稱、產業、市場別（上市／上櫃） |
| `daily_prices` | stock_id、date、開高低收、成交量（股）、成交金額 |
| `institutional_flows` | stock_id、date、外資／投信／自營的買進、賣出、淨額（股） |
| `margin` | stock_id、date、融資餘額、融券餘額 |
| `watchlist` | stock_id、標籤、備註 |
| `data_fetch_log` | 每次抓取的日期、成功或失敗、筆數（用來檢查缺資料） |

- 單位一律存「股」，只在顯示時換算成「張」（÷1000）。
- Schema 變更一律用遞增編號的 SQL migration 檔記錄。

## 開發階段

### 階段一（先做這個）

1. 建立專案骨架、GitHub repo，並連接 Vercel 與 Supabase。
2. 建立資料表與 migration 檔。
3. 寫抓資料腳本，並首次回補：追蹤股票池近 3 年的日 K 與法人資料。
4. 指標模組：MA、EMA、MACD、KD、RSI、布林通道、OBV、ATR。每個都要有單元測試，用已知數值驗證。
5. 個股頁：
   - 主圖：K 線 + 均線 + 成交量
   - 副圖：MACD／KD／RSI
   - 三大法人近 10／20 日明細

**完成標準：** 能查詢任一檔追蹤股，而且數字跟證交所或看盤軟體核對一致。

### 階段二

- 透明評分卡：每一項都能展開看原始數值。
- 條件篩選：在追蹤股票池中掃描底部或轉強條件。
- 追蹤清單，以及價格和指標警示。

### 階段三：回測

- 在伺服器端執行，使用資料庫裡的多年真實資料。
- 必須顯示樣本數、勝率、平均報酬、最大虧損，並跟同期「買進持有」比較。
- 樣本數少於 30 時，明確標示「樣本不足，僅供參考」。
- 計入交易成本：手續費 0.1425%（依券商折扣調整）、證交稅 0.3%。

## 已知的坑

**資料**
- FinMind 買賣超的單位是「股」，換算張數要 ÷1000。原版曾因此出現誇張到不合理的數字。
- 無成交日的收盤價可能是 0，計算前要先過濾掉。
- 除權息會造成價格跳空，指標和回測都要注意。之後評估是否改用還原股價。

**部署**
- Vercel 設定環境變數時，要勾選 Production。
- `SUPABASE_URL` 結尾不要加斜線。
- 推送前確認檔案結構正確，不要多包一層資料夾。

**安全**
- 任何 key 或 token 都不能寫進程式碼，也不能被 commit。
- `.env` 必須加進 `.gitignore`。

## 工作方式

- 動手前先提出計畫，等使用者確認後再開始。
- 一次只做一個小步驟。做完要告訴使用者怎麼驗證。
- 推送到 GitHub 或部署之前，先問使用者。
