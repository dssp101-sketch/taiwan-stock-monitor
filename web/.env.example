# 複製成 .env 後填入真實值。.env 已被 .gitignore 排除，絕對不要 commit。

# --- 前端會讀到的（VITE_ 開頭 = 會被打包進前端 bundle）---
# anon key 設計上就是公開的，真正的保護來自資料表的 RLS。
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=

# --- 只放伺服器端（Vercel Serverless / GitHub Actions Secrets）---
# 以下三個絕對不能加 VITE_ 前綴，否則會被打包進前端。
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
FINMIND_TOKEN=
